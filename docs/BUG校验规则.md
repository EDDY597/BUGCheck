# BUG 校验规则文档

> 定义 OpenProject BUG 校验工具的规则。  
> **改此文档后需同步修改 `validate-server.js`。**  
> 部分参数可在网页 **⚙ 设置** 中配置，不必改代码。

---

## 目录

1. [数据源](#1-数据源)
2. [类型分支](#2-类型分支)
3. [规则1：必填项（BUG）](#3-规则1必填项bug)
4. [规则2：人员（BUG）](#4-规则2人员bug)
5. [规则3：版本迭代（BUG）](#5-规则3版本迭代bug)
6. [规则4：模块最末级（BUG/建议）](#6-规则4模块最末级bug建议)
7. [规则5：关联工艺（BUG）](#7-规则5关联工艺bug)
8. [附录：模块层级](#8-附录模块层级)

---

## 1. 数据源

### 1.1 BUG 数据

- **平台**：OpenProject（默认 `https://op.crprobot.com`，可在设置中改列表页 URL）
- **列表页**：默认 `work_packages?query_id=4351`（作者=我，类型 7/37，最近 8 周）
- **登录**：设置中的账号/密码；cookie 缓存于 `cookies.json`

### 1.2 模块规格

- 来源：`module-data.js`（对应「CrobotpOS测试规格」三级层级）
- 匹配时忽略括号后缀：`送丝控制（R4）` → `送丝控制`

### 1.3 工作包类型

| 类型 | 校验 |
|:----:|------|
| **BUG** | 全部 5 项规则 |
| **需求** | 跳过 |
| **建议** | 精简：受理人、关联产品、负责人、发现BUG版本、模块、Release Phase、版本 |

---

## 2. 类型分支

- **BUG**：规则 1–5  
- **需求**：不校验  
- **建议**：见 1.3 精简字段；Release Phase：发现BUG版本多选 → `Product Release`，单选 → `DEV`；版本(Version) → `Product backlog`

---

## 3. 规则1：必填项（BUG）

以下 18 项必须有值（空或 `-` 均不通过）：

**描述内（1–7）**：现象描述、重现条件、最新的出厂版本是否能重现、重现概率、重现步骤、期望结果、实际结果  

**属性字段（8–18）**：受理人、负责人、Dev、Tester、关联产品、模块、缺陷类型、严重程度、发现BUG版本、Release Phase、版本

---

## 4. 规则2：人员（BUG）

| 字段 | 默认期望值（可在设置中改） |
|------|---------------------------|
| 负责人 | 黄贵良 |
| Tester | 刘杰 |

当前实现为 **包含匹配**（字段值包含期望姓名即通过）。

---

## 5. 规则3：版本迭代（BUG）

### 5.1 控制器版本

从描述「重现条件 → 软件版本 → 控制器版本」提取，形如 `CRA9-x.y.z-suffix`。

### 5.2 后缀 → Release Phase / Version

| 后缀包含 | 类型 | Release Phase | Version |
|----------|------|---------------|---------|
| `pre` / `rc` | RC | RC Release | Bug backlog |
| `alpha` | Alpha | Alpha Release | Bug backlog |
| `feature` / `feat` | Feature | Dev | 从控制器版本提取的 `1.x.y` |
| `beta` | Beta | Beta Release | Bug backlog |
| 其它（无上述关键词） | Stable | **Product Release** | **Bug backlog** |

### 5.3 版本链路（可配置）

默认从新到旧：`1.16.0 → 1.15.0 → 1.14.11 → 1.12.10`  
在 **⚙ 设置 → 版本链路** 中修改；计划任务 `run-once.vbs` 会读取 `data/login_config.json` 里同一配置。

| 条件 | 要求 |
|------|------|
| 当前控制器版本 | 必须出现在「发现BUG版本」 |
| 「能否重现」中 **否掉** 的旧版本 | **不能** 出现在「发现BUG版本」 |
| 未否掉的旧版本 | **必须** 出现在「发现BUG版本」 |

### 5.4 龙燕修改字段跳过校验

活动区 journal 操作人含「**龙燕**」或「**黄贵良**」时，解析其每条变更，抽出**被改过的字段名**，例如：

```
2026-09-12 11:31  版本 已从 Bug backlog 更改为 1.15.0   → 跳过「版本」
```

**规则（取代原先「有龙燕就放宽 Version」的整段豁免）：**

- 同一条活动可能含多个字段，按 `；` 拆分后逐个识别字段名  
  例：`受理人 已从 A 更改为 B；版本 已从 X 更改为 Y；Dev ...` → 跳过 受理人、版本、Dev
- **龙燕/黄贵良改过什么，就跳过什么**；没改过的仍按原规则校验
- 结果中不展示「受理人」「Dev」的修改明细（仍参与跳过）
- 其它字段以 `⏭️ 字段已被龙燕修改，跳过校验` 体现，并列出修改记录（输出文案仍用「龙燕」）

| 龙燕/黄贵良改过的字段 | 跳过的校验 |
|----------------|------------|
| 版本 / Version | 版本(Version) 期望值检查 |
| Release Phase | Release Phase 与控制器版本匹配 |
| 发现BUG版本 | 填写/格式，以及版本链路覆盖/排除 |
| 负责人 | 人员规则中的负责人 |
| Tester | 人员规则中的 Tester |
| 模块 | 最末级检查 |
| 其它必填字段 | 该字段的必填检查 |

---

## 6. 规则4：模块最末级（BUG/建议）

模块路径最后一段必须是叶子节点（三级规格，或无子级的二级/一级）。

| 情况 | 是否最末级 |
|------|:----------:|
| 三级规格 | 是 |
| 二级规格且无三级子级 | 是 |
| 二级规格且有子级 | 否，须选子级 |
| 一级规格且有子级 | 否，须选子级 |
| 一级规格且无子级 | 是 |

完整树见附录 / `module-data.js`。

---

## 7. 规则5：关联工艺（BUG）

**触发**：现象描述 / 重现步骤 / 重现条件 / 描述全文含下列任一关键词：  
`激光`、`弧焊`、`焊接`、`切割`、`点焊`、`折弯`、`中厚板`、`冲压`、`码垛`、`搬运`、`熔覆`

**要求**：「关联工艺」必须有值。

---

## 8. 附录：模块层级

> 与 `module-data.js` 同步维护。修改层级请改 JS，并视情况更新本节。

### 文件管理
文件列表、文件备份、文件恢复、软件升级、急救包

### 参数设置
操作参数、轴参数、速度参数、机构参数  
**驱动参数**：常用参数、电机参数  
**控制参数**：电机参数、碰撞参数、动力学、快捷摩擦力、减速器、平滑、精度、速度限制、滤波参数、软浮动参数、摆动参数  
机械单元、机械单元组、协作IO设置

### 功能设置
工具、用户、同步轴设置、零点、机器人标定、重力方向、干涉区、坐标系测量、变量配置表、碰撞检测预设、视觉功能、自定义事件、附加载荷、后台任务、软浮动预设、工装脱离、自定义界面  
**远程/预约**：远程、预约、程序号启动、--  
**通讯**：Modbus 客户端/主站/服务器/从站、TCP/IP、EtherCAT 主/伺服从站/IO从站/从(Anybus)、CanOpen、ProfileNet 主/从、CC-Link 主/IE从、EtherNetIP 主/从、DeviceNet主、ProfiNetSafe 主/从  
**跟踪功能**：直线跟踪、圆弧跟踪、地轨跟踪

### 系统信息
软件版本  
**伺服总线**：通讯状态、各轴状态  
**驱动**：编码器、驱动状态、模块信息

### 用户工艺
工艺快捷键、焊接微调、曲线导出、电弧跟踪、寻位工艺、激光跟踪、码垛工艺、偏移、鱼鳞焊、激光熔覆、飞拍工艺、协同、冲压/折弯工艺、机床上下料、打磨、压铸、喷涂、螺丝锁付、螺旋管、协作机器人  
**焊接工艺**：焊机配置、工艺参数、摆动工艺、多层多道、生产计数  
**激光焊接**：设备配置、工艺参数、摆动工艺、生产计数  
**专家数据库**：中厚板数据库、数据库管理、弧焊数据库  
**点焊工艺**：点焊配置、工艺参数、焊枪配置  
**激光切割**：激光配置、激光参数、图形参数

### PLC
PLC

### 监视
预约状态、时间、输入/输出、寄存器、位置变量  
**工艺监视**：激光/点焊/码垛/跟踪/焊接/焊机/焊机面板/生产计数/微调/冲压/电弧跟踪监视  
**坐标**：关节/世界/直角/用户坐标  
**速度**：关节速度、TCP速度  
**电机**：指令/反馈位置、电机速度、绝对位置、超差位置、转矩、干扰力

### 用户管理
账号管理、登录/注销

### 系统设置
时间加密、IP设置、主机名称、语言设置、系统时间、屏幕校准、无线网络、无线热点、硬件配置

### 程序
机器人/并行程序、编辑菜单、查看点位/变量、修改指令、中文编程、C/E型编辑界面  
**指令菜单**：运动/IO/控制/演算/特殊/焊接/点焊/码垛/视觉/跟踪/切割/其它/通讯指令

### 左右侧边栏
**C型左侧**：速度倍率、快捷回零、坐标系切换、M快捷键、伺服开关、清除报警、信息列表  
**C型右侧**：回零、轴键-GP/关节/笛卡尔、程序运行模式、机械单元切换、气检开关、送丝控制、手动焊接、焊接使能、焊接复位

### 状态栏
维护保养、紧急停止、节能模式、协同、机械单元、工具、用户、工作模式、时间、拖动状态、程序循环模式、速度倍率、坐标系、程序状态  
**控制栏**：热拔插

### FN
驱动参数读写、轨迹输出、程序偏移、轴禁止、临时屏蔽碰撞报警、碰撞/零位权限、焊点平移、时间加密、G10焊机参数导入

### 总线
MIII、EtherCAT

### 伺服
焊枪软浮动、固件升级

### 其他
外部引导/急停、限速限加速、外部轴、智慧云、输入法、UPS、重力补偿、节能模式、关节/步长滤波、三段叠加、平滑叠加、小距离加速、协作手柄、运动控制/学、动力学、机器人模型、轨迹规划、UI问题、系统启动、语言包、客户使用不当、其它、设备故障(broken)  
**产品出厂配置**：开机画面、默认参数、负载识别程序、系统PLC  
**软键盘**：全键盘、小键盘  
**日志系统**：示教器/控制器/按键/事件日志

### 示教器
安全/钥匙/急停开关、C型滚轮、E型按键、屏幕、正向/逆向/暂停运行、速度+/-  
**C型按键**：C1–C4  
**状态指示灯**：Power、Alarm  
**U盘接口**：U盘、外设

### BSP
**控制器**：CrobotpBSP-aarch64-Zhiyuan、aarch64-Tronlong、x64-Advan  
**示教器**：CrobotpBSP-armv7-Zhiyuan、armv7-Tronlong

### 电弧跟踪 / CrobotpLab
（无子级）

### 激光跟踪
激光跟踪、激光探头小板、VLS、VLS-SDK

### 智能化方案
油箱焊接、中储粮快检仪、轮毂打磨、管板智能焊接、船锚链、铁塔脚焊接、青藤多棱锥杆、豪特玻璃抓取、木工智能分拣(拆码垛)、建坤免示教、螺旋管

---

## 版本历史

| 版本 | 日期 | 内容 |
|:----:|:----:|------|
| v1.1 | 2026-09-14 | 补充可配置版本链路、人员设置、龙燕豁免说明；精简附录表述 |
| v1.0 | 2026-06-29 | 初始 5 项规则与模块层级 |
